# TP1 - IDS Network Analyzer

## Installation

```bash
pip install -r requirements.txt
```

## Utilisation

```bash
sudo python main.py -i eth0 -c 100 -o rapport.pdf
```

## Options

- `-i` : interface reseau
- `-c` : nombre de paquets
- `-o` : fichier PDF de sortie
